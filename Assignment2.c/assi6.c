//takes a character as an input and displays its ASCII code
#include <stdio.h>
int main()
{
char ch;
    printf("Enter any character =");
    scanf("%c",&ch);
    printf("ASCII code is %d",ch);
    return 0;
}